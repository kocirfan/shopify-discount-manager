query Input {
  cart {
    lines {
      id
    }
    attribute(key: "delivery_type") {
      value
    }
  }
  shop {
    deliveryDiscountSettings: metafield(
      namespace: "delivery_discount"
      key: "settings"
    ) {
      value
    }
  }
}